
# first 3 films + characters + planets + starships
{
  allFilms(first: 3) {
    films {
      title
      characterConnection { characters { name } }
      planetConnection { planets { name } }
      starshipConnection { starships { name } }
    }
  }
}
# (ran this in the playground)
